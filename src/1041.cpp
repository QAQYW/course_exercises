#include <bits/stdc++.h>
using namespace std;

// ! 没写完

const int N = 1000;

int T;
int n1, n2, a1[N], a2[N];

int main() {
    cin >> T;
    while (T--) {
        cin >> n1 >> n2;
        for (int i = 1; i <= n1; i++) cin >> a1[i];
        for (int i = 1; i <= n2; i++) cin >> a2[i];
        
    }


    return 0;
}